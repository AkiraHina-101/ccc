import os

# Output directory
output_dir = r"D:\GIF_Output"

# Current frequency
frequency = 437.2

# Viewports to export
viewports = [
    ("View 1", viewport_1),
    ("View 3", viewport_3),
    ("View 4", viewport_4),
    ("View 5", viewport_5),
]

# Create the output directory if it does not exist
if not os.path.exists(output_dir):
    os.makedirs(output_dir)

# Export each viewport as a separate GIF
for view_name, viewport in viewports:

    filename = "{:.1f} Hz_{}.gif".format(frequency, view_name)
    output_file = os.path.join(output_dir, filename)

    actranvi.play_animation(
        result_type='freq',
        start_first_result=True,
        harmonic_animation=True,
        frame_rate=24,
        result_step=3,
        number_of_cycles=3,
        save_movie=True,
        movie_filename=output_file,
        width=1199,
        height=876,
        background=False,
        legend=True,
        viewports=[viewport],
        loadcase='1',
        num_frames=363
    )

    print("Saved:", output_file)
